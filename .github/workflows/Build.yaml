name: Benchmark on Emulator

on:
  workflow_dispatch:

env:
  BASELINE_BRANCH: main
  GRADLE_BUILD_CACHE_DIR: ${{ github.workspace }}/.common-gradle-cache

jobs:
  benchmark-android:
    runs-on: ubuntu-latest
    strategy:
        fail-fast: false # if a runner fails the other can still go on
        matrix:
          include:
            - device: "pixel"
              api: 30
              profile: "pixel"
            - device: "Nexus 6"
              api: 29
              profile: "Nexus 6"
            - device: "Nexus 4"
              api: 28
              profile: "Nexus 4"
    timeout-minutes: 45

    steps:
      - name: Free Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf "/usr/local/share/boost"

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Checkout branch (Baseline)
        uses: actions/checkout@v4
        with:
          ref: ${{ env.BASELINE_BRANCH }}
          path: baseline

      - name: Checkout branch (Candidate)
        uses: actions/checkout@v4
        with:
          path: candidate

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          # By default, The setup-gradle action will only write to the cache from Jobs on the default (main/master) branch.
          # Jobs on other branches will read entries from the cache but will not write updated entries.
          cache-read-only: false

      - name: Build APKs (Baseline)
        working-directory: ./baseline
        run: |
          chmod +x ./gradlew
          ./gradlew :app:assembleDemoBenchmark

      - name: Build APKs (Candidate)
        working-directory: ./candidate
        run: |
          chmod +x ./gradlew
          ./gradlew :app:assembleDemoBenchmark :benchmarks:assembleDemoBenchmark

      - name: Run Benchmarks
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: ${{ matrix.api }}
          target: google_apis
          arch: x86_64
          profile: ${{ matrix.profile }}
          force-avd-creation: true
          emulator-options: -no-snapshot -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -memory 4096
          disable-animations: true
          script: |
            OUTPUT_FOLDER="./results_${{ matrix.device }}"
            adb install ./candidate/benchmarks/build/outputs/apk/demo/benchmarkRelease/benchmarks-demo-benchmarkRelease.apk
            chmod +x ./candidate/.github/scripts/run_macrobenchmarks.sh
            ./candidate/.github/scripts/run_macrobenchmarks.sh "./baseline/app/build/outputs/apk/demo/benchmarkRelease/app-demo-benchmarkRelease.apk" "./candidate/app/build/outputs/apk/demo/benchmarkRelease/app-demo-benchmarkRelease.apk" "$OUTPUT_FOLDER"

      - name: Compare macrobenchmark results
        run: |
            python3 ./candidate/.github/scripts/step_fit.py \
              "./results_${{ matrix.device }}/baseline" \
              "./results_${{ matrix.device }}/candidate"

      - name: Upload Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results-${{ matrix.device }}
          path: ./results_${{ matrix.device }}/

  collect-reports:
    if: always() 
    needs: [benchmark-android]
    runs-on: ubuntu-latest
    steps:
      - name: Download All Benchmark Results
        uses: actions/download-artifact@v4
        with:
          pattern: benchmark-results-*
          path: all_results
          # merge-multiple: true # merge so no nested folders

      - name: List Files (Debug)
        run: ls -R all_results

      - name: Upload Combined Report
        uses: actions/upload-artifact@v4
        with:
          name: FULL-BENCHMARK-REPORT
          path: all_results/